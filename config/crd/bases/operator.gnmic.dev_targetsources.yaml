---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: targetsources.operator.gnmic.dev
spec:
  group: operator.gnmic.dev
  names:
    kind: TargetSource
    listKind: TargetSourceList
    plural: targetsources
    singular: targetsource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TargetSource is the Schema for the targetsources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetSourceSpec defines the desired state of TargetSource
            properties:
              provider:
                properties:
                  http:
                    properties:
                      acceptPush:
                        type: boolean
                      authorization:
                        properties:
                          basic:
                            description: Enforce EITHER inline creds OR secret ref
                            properties:
                              credentialsSecretRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              password:
                                type: string
                              username:
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: either credentialsSecretRef OR both username
                                and password must be set, but not a mix
                              rule: (has(self.credentialsSecretRef) && !has(self.username)
                                && !has(self.password)) || (!has(self.credentialsSecretRef)
                                && has(self.username) && has(self.password))
                          jwt:
                            properties:
                              algorithm:
                                description: HS256, RS256, ES256, etc.
                                type: string
                              claims:
                                additionalProperties:
                                  type: string
                                description: 'Optional: generate JWT dynamically'
                                type: object
                              signingKeySecretRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              token:
                                description: Static pre-generated JWT
                                type: string
                              tokenSecretRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              ttl:
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: static JWT token and generated JWT configuration
                                cannot be combined
                              rule: '!((has(self.token) || has(self.tokenSecretRef))
                                && (has(self.signingKeySecretRef) || has(self.claims)))'
                            - message: static JWT token and generated JWT configuration
                                cannot be combined
                              rule: '!((has(self.token) || has(self.tokenSecretRef))
                                && (has(self.signingKeySecretRef) || has(self.claims)))'
                            - message: algorithm must be specified when generating
                                a JWT
                              rule: '!has(self.signingKeySecretRef) || self.algorithm
                                != ""'
                          token:
                            properties:
                              scheme:
                                minLength: 1
                                type: string
                              token:
                                type: string
                              tokenSecretRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            required:
                            - scheme
                            type: object
                            x-kubernetes-validations:
                            - message: either token or tokenSecretRef must be set,
                                but not both
                              rule: has(self.token) != has(self.tokenSecretRef)
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of the fields in [basic jwt token]
                            must be set
                          rule: '[has(self.basic),has(self.jwt),has(self.token)].filter(x,x==true).size()
                            == 1'
                      interval:
                        type: string
                      mapping:
                        description: JSONPath-style expressions
                        properties:
                          address:
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            type: object
                          name:
                            type: string
                          port:
                            type: string
                        required:
                        - address
                        - name
                        type: object
                      pagination:
                        properties:
                          itemsField:
                            description: 'Example: "results"'
                            type: string
                          nextField:
                            description: 'Example: "next"'
                            type: string
                        type: object
                      timeout:
                        type: string
                      tls:
                        properties:
                          caSecretRef:
                            description: SecretKeySelector selects a key of a Secret.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          insecureSkipVerify:
                            type: boolean
                        type: object
                      url:
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of the fields in [url acceptPush] must
                        be set
                      rule: '[has(self.url),has(self.acceptPush)].filter(x,x==true).size()
                        >= 1'
                type: object
                x-kubernetes-validations:
                - message: exactly one of the fields in [http] must be set
                  rule: '[has(self.http)].filter(x,x==true).size() == 1'
              targetLabels:
                additionalProperties:
                  type: string
                type: object
              targetProfile:
                minLength: 1
                type: string
            required:
            - provider
            - targetProfile
            type: object
          status:
            description: TargetSourceStatus defines the observed state of TargetSource
            properties:
              lastSync:
                format: date-time
                type: string
              status:
                type: string
              targetsCount:
                format: int32
                type: integer
            required:
            - lastSync
            - status
            - targetsCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
