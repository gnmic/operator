apiVersion: operator.gnmic.dev/v1alpha1
kind: Cluster
metadata:
  name: {{ .Name }}
spec:
  image: {{ .GnmicImage }}
  replicas: 1
  api:
    restPort: 7890
{{- if .GnmiPort }}
    gnmiPort: {{ .GnmiPort }}
{{- end }}
{{- if .TLSIssuer }}
    tls:
      issuerRef: {{ .TLSIssuer }}
{{- end }}
