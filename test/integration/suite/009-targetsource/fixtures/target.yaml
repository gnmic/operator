# A Target that belongs to a person, not a TargetSource.
apiVersion: operator.gnmic.dev/v1alpha1
kind: Target
metadata:
  name: {{ .Name }}
  labels:
    owner: human
spec:
  address: {{ .Address }}
  profile: default
