apiVersion: operator.gnmic.dev/v1alpha1
kind: Target
metadata:
  name: {{ .Name }}
  labels:
    suite: "010"
{{- if .Role }}
    role: {{ .Role }}
{{- end }}
spec:
  address: {{ .GnmiGenHost }}:{{ .Port }}
  profile: {{ .Profile }}
