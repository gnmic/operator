apiVersion: operator.gnmic.dev/v1alpha1
kind: Cluster
metadata:
  name: {{ .Name }}
spec:
  image: {{ .Image }}
  replicas: {{ .Replicas }}
  api:
    restPort: 7890
{{- if gt .PodCapacity 0 }}
  targetDistribution:
    podCapacity: {{ .PodCapacity }}
{{- end }}
